extends layout

block content
  .container
    H1 CREAR FECHA
    p(class="mb-5") Tomar todos los partidos con la misma fecha y crearla
        form(method="POST" id="createMatch" name="createMatch")
          #form-container
            .form-group
              label(for="round") Crear Fecha Nº
              input(type="number" name="round" class="form-control")
              input.button(type="submit" value="Enviar ➡" class="btn d-block w-50 mx-auto mt-5")